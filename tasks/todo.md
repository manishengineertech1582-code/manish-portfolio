# CreditFlow Card — Plan

## File
`app/page.tsx`

## Insertion Point
After line 267 (closing `</section>` of the `id="finrag-showcase"` section),
insert a new identical `<section>` block for CreditFlow before the `id="blog"` section (line 269).

## Changes
- Duplicate the FinRAG showcase section (lines 199–267)
- Update all FinRAG-specific values to CreditFlow equivalents
- No other file touched
